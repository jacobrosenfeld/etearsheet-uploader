# Google Service Account Credentials with Domain-Wide Delegation
# Create a service account at: https://console.cloud.google.com/iam-admin/serviceaccounts
# Enable Google Drive API and download the JSON key file
# Then copy the client_email and private_key fields here
# IMPORTANT: Grant the service account https://www.googleapis.com/auth/drive scope (full Drive access)
# for Shared Drive support. The narrower drive.file scope won't work with Shared Drives.
GOOGLE_SERVICE_ACCOUNT_EMAIL=your-service-account@your-project.iam.gserviceaccount.com
GOOGLE_SERVICE_ACCOUNT_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\nYourPrivateKeyHere\n-----END PRIVATE KEY-----\n"

# Email of the Google Workspace user to impersonate (must be in your domain)
# This user's storage quota will be used for uploads
GOOGLE_IMPERSONATE_USER=admin@yourdomain.com

# Optional: If using a Shared Drive folder, specify the Shared Drive ID
# You can find this in the URL when viewing the Shared Drive root
# Example: https://drive.google.com/drive/folders/0AOp8v3D... (the 0AOp8v3D... part)
GOOGLE_SHARED_DRIVE_ID=

# Portal Authentication
PORTAL_PASSWORD=your-portal-password-here
ADMIN_PASSWORD=your-admin-password-here
SESSION_SECRET=your-long-random-session-secret-here

# Vercel Blob Storage (for configuration and admin state)
# Get this token from Vercel dashboard: Storage -> Blob -> Generate Token
BLOB_READ_WRITE_TOKEN=vercel_blob_rw_...

# Optional: Custom blob keys for storage (uses defaults if not specified)
APP_CONFIG_BLOB_KEY=config/upload-portal-config.json
ADMIN_STATE_BLOB_KEY=admin-state/admin-state.json

# App URL
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Service account email for display in admin panel (same as GOOGLE_SERVICE_ACCOUNT_EMAIL)
NEXT_PUBLIC_SERVICE_ACCOUNT_EMAIL=your-service-account@your-project.iam.gserviceaccount.com


# Optional: Drive config folder ID (app creates one if omitted)
APP_CONFIG_FOLDER_NAME=UploadPortalConfig
APP_CONFIG_FILE_NAME=upload-portal-config.json